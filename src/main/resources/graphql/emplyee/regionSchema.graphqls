type Regions {
    id: ID!
    name: String!
}

extend type Query {
    getAllRegions: [Regions]
    getRegionById(id: ID!): Regions
}

extend type Mutation {
    createRegion(name: String!): Regions
    updateRegion(id: ID!, name: String!): Regions
    deleteRegion(id: ID!): Boolean
}